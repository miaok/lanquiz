import 'package:flutter/material.dart';
import 'quiz_app.dart';

/// 知识竞答游戏启动入口
///
/// 运行方式：
/// flutter run -t lib/quiz/main_quiz.dart
///
/// 或者在main.dart中引入：
/// import 'quiz/quiz_app.dart';
///
/// 然后在main()中运行：
/// runApp(const QuizApp());
void main() {
  runApp(const QuizApp());
}
